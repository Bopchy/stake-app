import React from "react";

const Header = ({ name }) => (
  <>
    <p>Lock {name} to Boost Rewards</p>
    <p>Locking {name} will increase yield and voting power</p>
  </>
);

export default Header;
